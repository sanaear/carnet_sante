{% extends 'medecin/base.html.twig' %}

{% block title %}Nouvelle Consultation{% endblock %}

{% block content %}
    <div class="px-4 sm:px-6 lg:px-8">
        <div class="sm:flex sm:items-center">
            <div class="sm:flex-auto">
                <h1 class="text-xl font-semibold text-gray-900">Nouvelle Consultation</h1>
                <p class="mt-2 text-sm text-gray-700">Créer une nouvelle consultation pour un patient.</p>
            </div>
        </div>

        <div class="mt-8">
            {{ include('medecin/consultation/_form.html.twig') }}
        </div>

        <div class="mt-4">
            <a href="{{ path('medecin_dashboard') }}" class="text-sm font-medium text-blue-600 hover:text-blue-500">
                Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}